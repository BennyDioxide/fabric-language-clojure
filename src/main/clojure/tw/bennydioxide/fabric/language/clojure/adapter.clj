(ns tw.bennydioxide.fabric.language.clojure.adapter
  (:gen-class
    :name tw.bennydioxide.fabric.language.clojure.adapter.ClojureAdapter
    :implements [net.fabricmc.loader.api.LanguageAdapter]
    :main false
    :prefix "-"))

(defn -create [mod value type]
  ())
